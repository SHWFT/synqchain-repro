# Adapter selection: mock (default), none (empty responses), later: epicor, netsuite, sap
ERP_PROVIDER=mock

# If 1, seed the mock adapter with sample data on first run. If 0/absent, return empty arrays/zeros.
MOCK_SEED=0

# Base URL for client fetch wrapper (optional, can be relative)
NEXT_PUBLIC_API_BASE=